diff --git a/src/main/java/me/arcaniax/hdb/libs/kyori/adventure/permission/PermissionCheckers.java b/src/main/java/me/arcaniax/hdb/libs/kyori/adventure/permission/PermissionCheckers.java
deleted file mode 100644
index 5f43eea..0000000
--- a/src/main/java/me/arcaniax/hdb/libs/kyori/adventure/permission/PermissionCheckers.java
+++ /dev/null
@@ -1,50 +0,0 @@
-package me.arcaniax.hdb.libs.kyori.adventure.permission;
-
-import me.arcaniax.hdb.libs.kyori.adventure.util.TriState;
-import org.jetbrains.annotations.NotNull;
-import org.jetbrains.annotations.Nullable;
-
-final class PermissionCheckers {
-   static final PermissionChecker NOT_SET = new PermissionCheckers.Always(TriState.NOT_SET);
-   static final PermissionChecker FALSE = new PermissionCheckers.Always(TriState.FALSE);
-   static final PermissionChecker TRUE = new PermissionCheckers.Always(TriState.TRUE);
-
-   private PermissionCheckers() {
-   }
-
-   private static final class Always implements PermissionChecker {
-      private final TriState value;
-
-      private Always(final TriState value) {
-         this.value = valuex;
-      }
-
-      @NotNull
-      @Override
-      public TriState value(@NotNull final String permission) {
-         return this.value;
-      }
-
-      @Override
-      public String toString() {
-         return PermissionChecker.class.getSimpleName() + ".always(" + this.value + ")";
-      }
-
-      @Override
-      public boolean equals(@Nullable final Object other) {
-         if (this == other) {
-            return true;
-         } else if (other != null && this.getClass() == other.getClass()) {
-            PermissionCheckers.Always var2 = (PermissionCheckers.Always)other;
-            return this.value == var2.value;
-         } else {
-            return false;
-         }
-      }
-
-      @Override
-      public int hashCode() {
-         return this.value.hashCode();
-      }
-   }
-}
